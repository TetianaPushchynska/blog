/ Main Header
%header.main-header
  / Logo
  / Header Navbar
  %nav.navbar.navbar-static-top
    / Sidebar toggle button
    / Navbar Right Menu
    .navbar-custom-menu
      .nav.navbar-nav
        / Notifications Menu
        .dropdown.notifications-menu
          / Menu toggle button
          .hidden-xs=current_user.email if current_user
          .pull-right
            -if user_signed_in?
              = link_to t('user.sign_out'), destroy_user_session_path, method: :delete, class: 'btn btn-default btn-flat'
